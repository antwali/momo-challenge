# Test environment (NODE_ENV=test is set by Jest)
# Copy to .env.test and adjust. Use a separate DB to avoid wiping dev data.

DATABASE_URL="postgresql://momo:momo@localhost:5432/momo_wallet_test?schema=public"
NODE_ENV=test
NOTIFICATION_CHANNEL=console
