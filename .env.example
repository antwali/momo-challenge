# Database (used by Prisma and app)
DATABASE_URL="postgresql://momo:momo@localhost:5432/momo_wallet?schema=public"

# Server
PORT=3000
NODE_ENV=development

# Optional: notification mock (e.g. log-only)
NOTIFICATION_CHANNEL=console
